export const messageIds = {
  missingFieldSemicolon:
    'Field `{{name}}` must end with a semicolon before a computed member',
  missingLeadingSemicolon:
    'Computed member `{{name}}` must start with a semicolon',
}
